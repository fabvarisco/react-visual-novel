
export const GAME_CSS_DEFAULT = `.container {
  width: 100%;
  margin-left: 150px;
}
`;

export const STYLES_CSS_DEFAULT = `button {
  justify-content: flex-start;
  margin-left: 60px;
}
`;

export const MORE_STYLES_CSS_DEFAULT = `.card {
  position: relative;
  left: 120px;
  transform: rotate(-3deg);
}

.card p {
  text-align: right;
}

.card img {
  transform: rotate(12deg) scale(1.4, 0.75) skewX(-10deg);
}
`;
